
(function (modules) { // 转换数据
	let installedModlues = {}
	function __webpcak_require__(moduleId) {
		if (installedModlues[moduleId]) { // 查看是否有缓存
			return installedModlues[moduleId].exports
		}
		var module = installedModlues[moduleId] = {
			i: moduleId,
			l: false,
			exports: {}
		}
		modules[moduleId].call(moduel.exports, module, module.exports, __webpcak_require)
		module.l = true
		return module.exports
	}
	return __webpcak_require(__webpcak_require__.s = "<%-entryId%>")
})({
	<%for (let key in modules) {%>
	  	"<%-key%>":
	  	(function (module, exports, __webpack_require__) {
	  		eval(`<%-modules[key]%>`);
		  }), 
	<%}%>
})